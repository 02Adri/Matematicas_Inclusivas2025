import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.matematicainclusiva',
  appName: 'matematicainclusiva',
  webDir: 'www'
};

export default config;
